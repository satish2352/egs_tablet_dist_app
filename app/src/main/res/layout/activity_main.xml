<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.sipl.egstabdistribution.ui.activities.BeneficiaryListActivity">

    <TextView
        android:layout_marginTop="10dp"
        android:layout_centerInParent="true"
        android:id="@+id/tvNoRecords"
        android:text="No Records Found"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"></TextView>
   <LinearLayout
       android:id="@+id/mainLayout"
       android:orientation="vertical"
       android:layout_width="match_parent"
       android:layout_height="match_parent">
       <androidx.recyclerview.widget.RecyclerView
           android:layout_weight="1"
           android:layout_margin="8dp"
           android:id="@+id/recyclerView"
           android:scrollbarSize="5dp"
           android:scrollbars="vertical"
           android:scrollbarThumbVertical="@color/appBlue"
           android:scrollbarAlwaysDrawVerticalTrack="true"
           android:layout_width="match_parent"
           android:layout_height="wrap_content"></androidx.recyclerview.widget.RecyclerView>
       <LinearLayout
           android:layout_gravity="center"
           android:layout_margin="8dp"
           android:layout_width="match_parent"
           android:layout_height="wrap_content"
           android:orientation="horizontal">
           <androidx.recyclerview.widget.RecyclerView
               android:layout_gravity="center"
               android:scrollbars="horizontal"
               android:scrollbarThumbHorizontal="@color/appBlue"
               android:scrollbarTrackHorizontal="@color/appBlue"
               android:scrollbarAlwaysDrawHorizontalTrack="true"
               android:layout_weight="1"
               android:id="@+id/recyclerViewPageNumbers"
               android:layout_width="match_parent"
               android:layout_height="wrap_content"></androidx.recyclerview.widget.RecyclerView>
           <include layout="@layout/layout_scrollable_icon"  android:visibility="gone" android:id="@+id/layoutIconScroll"/>
           <com.google.android.material.floatingactionbutton.FloatingActionButton
               android:layout_gravity="center"
               android:visibility="gone"
               android:id="@+id/floatingActionButton"
               android:layout_width="wrap_content"
               android:layout_height="wrap_content"
               android:src="@drawable/add"
               android:text="Add"
               android:backgroundTint="@color/appBlue" />
       </LinearLayout>

   </LinearLayout>
</RelativeLayout>